  Задача. Написать парсер рекурсивным спуском
  для подмножества Scheme:

(define (f x y)
  (if (> x y)
      (cond ((> x 0) (- x)
            ((< y 0) (/ y))))
      (let ((a 1)
            (b 2)
        (* a b)))))

(define x (+ 100 500))

(define (f x)
  (lambda (y) (+ x y)))